# União das duas listas que colidiram no merge ad08c60.
# Versões fixadas de propósito: o container precisa ser reprodutível, senão o
# build de hoje e o de daqui a três meses instalam coisas diferentes.

fastapi==0.115.6
pydantic==2.10.4

# Lambda: Mangum traduz o evento do Function URL para ASGI.
mangum==0.19.0

# Desenvolvimento local: uvicorn query_engine.main:app --reload
uvicorn[standard]==0.34.0

pandas==2.2.3
numpy==2.2.1

google-api-python-client==2.157.0
google-auth==2.37.0
